import { NextResponse } from "next/server";
import { requireAdmin } from "@/lib/adminGuard";
import { prisma } from "@/lib/prisma";
import { skuPatchSchema } from "@/lib/validation/catalog";
import { audit, requireUnchanged, validationError } from "@/server/admin/mutation";
type Context = { params: Promise<{ id: string; skuId: string }> };
export async function PATCH(req: Request, { params }: Context) { const admin = await requireAdmin(req); if (!admin.ok) return admin.response; const parsed = skuPatchSchema.safeParse(await req.json()); if (!parsed.success) return NextResponse.json(validationError(parsed.error), { status: 400 }); const { id, skuId } = await params; const before = await prisma.sku.findFirst({ where: { id: skuId, protocolId: id }, include: { aliases: true } }); if (!before) return NextResponse.json({ error: "SKU não encontrado." }, { status: 404 }); const stale = requireUnchanged(req, before.updatedAt); if (stale) return stale; const { aliases, ...data } = parsed.data; if (aliases) { const removed = before.aliases.map((item) => item.alias).filter((alias) => !aliases.includes(alias)); if (await prisma.orderItem.count({ where: { productId: { in: removed } } })) return NextResponse.json({ error: "Apelido em uso não pode ser removido." }, { status: 409 }); } const sku = await prisma.$transaction(async (tx) => { if (aliases) { await tx.skuAlias.deleteMany({ where: { skuId } }); if (aliases.length) await tx.skuAlias.createMany({ data: aliases.map((alias) => ({ alias, skuId })) }); } return tx.sku.update({ where: { id: skuId }, data, include: { aliases: true } }); }); await audit(admin.user.userId, "Sku", skuId, "update", { before, after: sku }); return NextResponse.json({ sku }); }
export async function DELETE(req: Request, { params }: Context) { const admin = await requireAdmin(req); if (!admin.ok) return admin.response; const { id, skuId } = await params; const sku = await prisma.sku.findFirst({ where: { id: skuId, protocolId: id }, include: { aliases: true } }); if (!sku) return NextResponse.json({ error: "SKU não encontrado." }, { status: 404 }); const sold = await prisma.orderItem.count({ where: { productId: { in: [sku.code, ...sku.aliases.map((alias) => alias.alias)] } } }); if (sold) { const archived = await prisma.sku.update({ where: { id: skuId }, data: { isActive: false } }); return NextResponse.json({ success: true, archived: true, sku: archived }); } await prisma.skuAlias.deleteMany({ where: { skuId } }); await prisma.sku.delete({ where: { id: skuId } }); await audit(admin.user.userId, "Sku", skuId, "delete", {}); return NextResponse.json({ success: true }); }
