import { NextResponse } from "next/server";
import { requireAdmin } from "@/lib/adminGuard";
import { prisma } from "@/lib/prisma";
import { audit } from "@/server/admin/mutation";
export async function POST(req: Request, { params }: { params: Promise<{ id: string }> }) { const admin = await requireAdmin(req); if (!admin.ok) return admin.response; const id = (await params).id; const product = await prisma.product.findUnique({ where: { id }, include: { skus: { where: { isActive: true } }, images: true } }); if (!product) return NextResponse.json({ error: "Produto não encontrado." }, { status: 404 }); if (!product.name || !product.summary || !product.presentation || !product.skus.length || !product.images.length) return NextResponse.json({ error: "Preencha os campos obrigatórios, uma imagem e ao menos um SKU ativo." }, { status: 400 }); const updated = await prisma.product.update({ where: { id }, data: { status: "PUBLISHED", publishedAt: new Date() } }); await audit(admin.user.userId, "Product", id, "publish", { status: "PUBLISHED" }); return NextResponse.json({ product: updated }); }
